#include <iostream>

int main() {
    
    int students = 21;

    // students = students + 1;
    // students+= 1;
    // students++;

    // students - 1;
    // students -= 1;
    // students --;

    // students = students * 2;
    // students *=2;


    // students = students / 2;
    // students/= 2;

    // int remainder = students % 2;

    // c++ operations 

    // int students = 6 - 5 + (4 * 3) / 2;

    std::cout << students << std::endl;
    return 0;
}